const personas=['killiam','dani','nika'];
personas.forEach(function(persona, index){
    // console.log(index);
    console.log(persona);
});

personas.forEach((persona, index)=>{
    console.log(persona);
});
